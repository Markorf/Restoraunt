import { makeStyles } from "@material-ui/styles";
export default makeStyles(() => ({
  root: {
    position: "absolute",
    backgroundColor: "#3f51b5",
    color: "white",
    bottom: 0,
    left: 0,
    right: 0,
    padding: ".3rem"
  }
}));
